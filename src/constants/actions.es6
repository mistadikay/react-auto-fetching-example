export default Object.freeze({
    // product info
    PRODUCT_INFO_LOADED: Symbol(),
    PRODUCT_INFO_LOAD_ERROR: Symbol(),

    // products list
    PRODUCTS_LOADED: Symbol(),
    PRODUCTS_LOAD_ERROR: Symbol()
});
